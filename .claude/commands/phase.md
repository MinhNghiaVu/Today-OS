---
description: Start a numbered phase per the Session Protocol in CLAUDE.md
argument-hint: [phase-number]
---

I'm starting Phase $ARGUMENTS of the Today OS project.

Follow the Session Protocol in CLAUDE.md exactly. In particular:
1. Read CLAUDE.md and the spec sections relevant to this phase.
2. Propose a 3–4 step micro-plan, then execute it.
3. For anything requiring my manual action (API keys, OAuth, buckets, billing), write `docs/phase$ARGUMENTS-setup.md` with clear, copy-paste-ready steps. Use MCPs to do setup yourself wherever possible.
4. Watch context: at ~80% run /compact; when switching tasks run /clear.
5. End of session: update Task Queue, Decisions, Log, and Open Questions in CLAUDE.md, then run /caveman-commit.
